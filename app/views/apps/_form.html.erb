<%= form_for(@app) do |f| %>
  <% if @app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% @app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :dev_lead %><br />
    <%= f.text_field :dev_lead %>
  </div>
  <div class="field">
    <%= f.label :support_lead %><br />
    <%= f.text_field :support_lead %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
